// projects/react/18/src/app/router/router.tsx

import { createBrowserRouter } from 'react-router';
import { routeConfig } from './route-config';

export const router = createBrowserRouter(routeConfig);
