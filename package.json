{
  "name": "experience",
  "private": true,
  "version": "0.0.1-dev",

  "scripts": {
    "reset": "bash scripts/reset-package.sh",
    "format": "prettier --write ."
  },

  "devDependencies": {
    "prettier": "3.9.5"
  }
}
