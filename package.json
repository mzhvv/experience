{
  "name": "experience",
  "private": true,
  "version": "0.0.1-dev",

  "scripts": {
    "package-reset": "bash scripts/package/removing.sh && bash scripts/package/installing.sh",
    "format": "prettier --write ."
  },

  "devDependencies": {
    "prettier": "3.9.5"
  }
}
