<!-- packages/libs/README.md -->

# @mzhvv/libs

# Структура

@mzhvv/libs:

- Домен (e.g.: `npm`)
  - bin - CLI-команды
  - core - Сценарии
  - libs - Методы
    - Экспорт: матрица (e.g.: `npmLibs.publish()`) и прямой импорт (e.g.: `publishNpm()`)
